from robobosim.RoboboSim import RoboboSim
from robobopy.Robobo import Robobo
from dataset_utils import collect_dataset
from merge_csv import merge_csv_files
import os
    
def main():
    sim = RoboboSim('localhost'); sim.connect(); sim.wait(0.5)
    rob = Robobo('localhost'); rob.connect(); rob.wait(0.5)

    print("=== Recolectando dataset SIMPLE ===")
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="1.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="2.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="3.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="4.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="5.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="6.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="7.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="8.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="9.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="10.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="11.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="12.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="13.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="14.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="15.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="16.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="17.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="18.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="19.csv", simple=True)
    sim.resetSimulation()
    sim.wait(3)
    rob.moveTiltTo(110,20)
    rob.moveTiltTo(90,20)
    collect_dataset(rob, sim, n_samples=400, export_name="20.csv", simple=True)
    
    
    sim.disconnect()
    rob.disconnect()
    
    merge_csv_files(input_dir="datasets/", output_file="datasets/merge.csv", start=1, end=20)

if __name__ == '__main__':
    main()
